name: RAAM tracker poll every 5 minutes

on:
  schedule:
    - cron: "*/5 * * * *"
  workflow_dispatch: {}

permissions:
  contents: read

jobs:
  poll:
    runs-on: ubuntu-latest
    steps:
      - name: Hit /api/cron/tracker
        env:
          CRON_SECRET: ${{ secrets.CRON_SECRET }}
        run: |
          if [ -z "$CRON_SECRET" ]; then
            echo "::warning::CRON_SECRET secret not set — tracker cron disabled"
            exit 0
          fi
          code=$(curl -s -o /tmp/out.txt -w "%{http_code}" \
            -H "Authorization: Bearer $CRON_SECRET" \
            https://raam-2026.vercel.app/api/cron/tracker)
          echo "HTTP $code"
          head -c 4000 /tmp/out.txt || true
          test "$code" = "200"
